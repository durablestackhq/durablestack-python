[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "durablestack"
version = "0.1.0b4"
description = "DurableStack Python runtime"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "DurableStack" }
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: System :: Distributed Computing",
  "Topic :: Software Development :: Libraries"
]
keywords = [
  "durable-jobs",
  "scheduling",
  "background-workers",
  "job-queue",
  "distributed-systems"
]
dependencies = [
  "asyncpg>=0.30.0",
  "pymysql>=1.1.1",
  "pyodbc>=5.1.0",
  "croniter>=3.0.3",
  "tzdata>=2024.1"
]

[project.urls]
Homepage = "https://durablestack.com"
Repository = "https://github.com/durablestack/durablestack-python"
Issues = "https://github.com/durablestack/durablestack-python/issues"

[project.optional-dependencies]
dev = [
  "build>=1.2.2",
  "twine>=5.1.1",
  "pytest>=8.0.0",
  "pytest-asyncio>=0.24.0",
  "ruff>=0.6.0",
  "mypy>=1.11.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
ignore = ["S101"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
strict = true
packages = ["durablestack"]
[[tool.mypy.overrides]]
module = ["croniter"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["asyncpg"]
ignore_missing_imports = true

[tool.hatch.build.targets.wheel]
packages = ["src/durablestack"]
